  <li id="<%= test_id %>" class="<%= failed ? 'failed' : complete ? 'passed' : 'incomplete' %>">
    (<%= email || "anonymous" %>) - <a
    target="_blank" href="<%= url %>"><%= url %></a> <%
    if(typeof passed !== "undefined") { %> (<%= passed %>/<%= failed %>/<%=
      total %><% if(typeof runtime !== "undefined") { %>/<%= runtime %>ms<% }
      %>) <% } %>

    <% if (complete && failed_tests.length) { %>
      <h5>Failed Tests</h5>

      <ul>
        <% failed_tests.forEach(function(name, index) { %>
          <li>
            <%= name %>
          </li>
        <% }); %>
      </ul>
    <% } %>
  </li>
